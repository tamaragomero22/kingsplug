[phases.setup]
nixPkgs = ['nodejs_22']  # Changed from nodejs_20 to nodejs_22

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npx serve -s dist -l $PORT'